import React from 'react';

// PUBLIC_INTERFACE
export default function UserMenu() {
  /** Simple user menu stub. Replace with real menu when auth exists. */
  return (
    <button className="btn ghost" aria-haspopup="menu" aria-label="User menu">
      👤 User
    </button>
  );
}
